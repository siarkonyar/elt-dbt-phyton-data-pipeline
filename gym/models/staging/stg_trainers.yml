version: 2

models:
  - name: stg_trainers
    description: "row trainers data."
    columns:
      - name: trainer_id
        description: "Primary key for the trainer."
        tests:
          - unique
          - not_null

      - name: email
        tests:
            - unique
            - not_null