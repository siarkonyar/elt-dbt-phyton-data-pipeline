# Copy this file to `.env` and fill in your own values:
#   cp elt_script/.env.example elt_script/.env
#
# These must match POSTGRES_PASSWORD for each service in docker-compose.yaml.

SOURCE_POSTGRES_PASSWORD=your_source_password_here
DESTINATION_POSTGRES_PASSWORD=your_destination_password_here
