gym:
  target: dev
  outputs:
    dev:
      type: postgres
      # Docker service name and internal port — not localhost:5434,
      # which only works from the host machine.
      host: destination_postgres
      port: 5432
      database: destination_db
      schema: public
      user: postgres
      password: secret
      threads: 1
